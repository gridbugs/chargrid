[package]
name = "tetris_graphical"
version = "0.1.0"
authors = ["Stephen Sherratt <stephen@sherra.tt>"]
edition = "2018"

[dependencies]
chargrid_graphical = { path = "../../../graphical" }
tetris_app = { path = "../app" }
rand = "0.7"
env_logger = "0.8"
