[package]
name = "tetris_app"
version = "0.1.0"
authors = ["Stephen Sherratt <stephen@sherra.tt>"]
edition = "2021"

[dependencies]
tetris = { path = "../tetris" }
chargrid = { path = "../../../chargrid", features = ["gamepad"] }
rand = "0.8"
rand_isaac = "0.3"
