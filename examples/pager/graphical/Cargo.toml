[package]
name = "pager_graphical"
version = "0.1.0"
authors = ["Stephen Sherratt <stephen@sherra.tt>"]
edition = "2018"

[dependencies]
env_logger = "0.8"
chargrid_graphical = { path = "../../../graphical" }
pager_app = { path = "../app" }
